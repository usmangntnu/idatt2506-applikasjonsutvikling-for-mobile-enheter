<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Oving_2a">

        <!-- MainActivity: Aktivitet som genererer et tilfeldig tall. (oppgave b,c,d) -->
        <!-- Den startes via et implisitt Intent med action ACTION_GET_NUMBER. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.oving_2a.ACTION_GET_NUMBER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- RandomActivity: Test-applikasjon som starter MainActivity. (oppgave e) -->
        <!-- Dette er LAUNCHER-aktiviteten for appen. -->
        <activity
            android:name=".RandomActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
