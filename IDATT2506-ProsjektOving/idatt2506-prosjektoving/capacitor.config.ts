import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'idatt2506-prosjektoving',
  webDir: 'dist'
};

export default config;
